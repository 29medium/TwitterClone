<p id="notice"><%= notice %></p>

<h1>People</h1>

<table>
  <thead>
  <tr>
    <th>User</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @users.each do |user| %>
    <tr class="<%= cycle('list_line_odd', 'list_line_even')%>">
      <td><%= user.username %></td>
      <td><%= link_to 'Show', user_path(user.id) %></td>
    </tr>
  <% end %>
  </tbody>
</table>